{
  "name": "rowrunner-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx ExampleHTTPServer.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "producer": "tsx ExampleProgressProducer.ts",
    "predev": "npm run build --prefix ..",
    "prebuild": "npm run build --prefix ..",
    "pretypecheck": "npm run build --prefix .."
  },
  "dependencies": {
    "rowrunner": "file:..",
    "three": "^0.186.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "^0.186.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
